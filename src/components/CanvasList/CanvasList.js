import React from 'react';

const CanvasList = (props) => {
  return (
    <div>
      <h1>Select a canvas</h1>
    {/* Maybe map over a list of the svgs passed in via props? */}
    </div>
  )
}

export default CanvasList